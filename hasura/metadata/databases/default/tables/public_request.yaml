table:
  name: request
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - auth
        - created_at
        - session_id
        - status
        - updated_at
      filter: {}
      allow_aggregations: true
    comment: ""
